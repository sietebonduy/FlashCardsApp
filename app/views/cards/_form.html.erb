<%= form_with(model: card) do |form| %>
  <% if card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
        <% card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :front_side, style: "display: block" %>
    <%= form.text_field :front_side %>
  </div>

  <div>
    <%= form.label :back_side, style: "display: block" %>
    <%= form.text_field :back_side %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
